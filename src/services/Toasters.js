import React, { Component, Fragment } from "react";
import ToastersList from "../components/Users/ToastersList";

class Toasters extends Component {
    render() {
        return (
            <Fragment>
                <ToastersList/>
            </Fragment>
        );
    }
}

export default Toasters;
