name: heroku-deploy

on:
  push:
    branches: main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.4.6
        with:
          heroku_api_key: ${{secrets.HEROKU_API_KEY}}
          heroku_app_name: "coffaine-frontend"
          heroku_email: "${{secrets.HEROKU_EMAIL}}"
          usedocker: true
        env:
          HD_REACT_APP_API_ENDPOINT: ${{secrets.REACT_APP_API_ENDPOINT}}
